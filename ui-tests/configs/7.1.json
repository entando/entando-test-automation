{
  "_instructions": "Replace {YOUR_ENTANDO_BASE_URL} in baseUrl, restAPI, and auth_base_url with the base URL of your Entando environment. Replace {YOUR_API_CLIENT_ID} and {YOUR_API_CLIENT_SECRET} with your Api client ID and secret respectively.",
  "baseUrl": "http://{YOUR_ENTANDO_BASE_URL}",
  "basePath": "/app-builder",
  "portalUIPath": "/entando-de-app/en",
  "restAPI": "http://{YOUR_ENTANDO_BASE_URL}/entando-de-app/api",
  "adminConsolePath": "http://{YOUR_ENTANDO_BASE_URL}/entando-de-app/do",
  "env": {
    "auth_base_url": "http://{YOUR_ENTANDO_BASE_URL}/auth",
    "auth_realm": "entando",
    "auth_client_id": "entando-web",
    "api_client_id": "{YOUR_API_CLIENT_ID}",
    "api_client_secret": "{YOUR_API_CLIENT_SECRET}"
  }
}